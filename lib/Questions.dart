class Question {
    String? questionBank;
    bool? questionAnswer;

    Question({
        this.questionBank,
        this.questionAnswer
    });
    // Question(this.questionBank, this.questionAnswer);

}